import 'package:flutter/material.dart';

class InitalHomePage extends StatelessWidget {
  const InitalHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("My period is about to start"),
      ),
    );
  }
}
